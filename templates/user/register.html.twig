{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    
    <button type="submit">{% trans %}user.create.confirm{% endtrans %}</button>
    
    {{ form_end(form) }}
{% endblock %}