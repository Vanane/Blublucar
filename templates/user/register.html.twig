{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    
    <button type="submit">Créer un compte</button>
    
    {{ form_end(form) }}
{% endblock %}